#ifndef TIMECONVERTION_H
#define TIMECONVERTION_H

class TimeConvertion {
public:
    TimeConvertion();

    static int minutesToMiliseconds(const float& minutes);
    static int milisecondsToMinutes(const float& miliseconds);
};

#endif // TIMECONVERTION_H
